// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/encodings/table_schema.def.rs".

#include "table_schema.hpp"

#include "table_group.hpp"
#include "table_sort_scope.hpp"

#include <arrow/builder.h>
#include <arrow/type_fwd.h>

namespace rerun::encodings {}

namespace rerun {
    const std::shared_ptr<arrow::DataType>& Loggable<encodings::TableSchema>::arrow_data_type() {
        static const auto datatype = arrow::struct_({
            arrow::field(
                "groups",
                arrow::list(arrow::field(
                    "item",
                    Loggable<rerun::encodings::TableGroup>::arrow_data_type(),
                    false
                )),
                false
            ),
            arrow::field(
                "sort_scope",
                Loggable<rerun::encodings::TableSortScope>::arrow_data_type(),
                true
            ),
            arrow::field("sticky_columns", arrow::uint32(), true),
        });
        return datatype;
    }

    Result<std::shared_ptr<arrow::Array>> Loggable<encodings::TableSchema>::to_arrow(
        const encodings::TableSchema* instances, size_t num_instances
    ) {
        // TODO(andreas): Allow configuring the memory pool.
        arrow::MemoryPool* pool = arrow::default_memory_pool();
        auto datatype = arrow_data_type();

        ARROW_ASSIGN_OR_RAISE(auto builder, arrow::MakeBuilder(datatype, pool))
        if (instances && num_instances > 0) {
            RR_RETURN_NOT_OK(Loggable<encodings::TableSchema>::fill_arrow_array_builder(
                static_cast<arrow::StructBuilder*>(builder.get()),
                instances,
                num_instances
            ));
        }
        std::shared_ptr<arrow::Array> array;
        ARROW_RETURN_NOT_OK(builder->Finish(&array));
        return array;
    }

    rerun::Error Loggable<encodings::TableSchema>::fill_arrow_array_builder(
        arrow::StructBuilder* builder, const encodings::TableSchema* elements, size_t num_elements
    ) {
        if (builder == nullptr) {
            return rerun::Error(ErrorCode::UnexpectedNullArgument, "Passed array builder is null.");
        }
        if (elements == nullptr) {
            return rerun::Error(
                ErrorCode::UnexpectedNullArgument,
                "Cannot serialize null pointer to arrow array."
            );
        }

        {
            auto field_builder = static_cast<arrow::ListBuilder*>(builder->field_builder(0));
            auto value_builder = static_cast<arrow::StructBuilder*>(field_builder->value_builder());
            ARROW_RETURN_NOT_OK(field_builder->Reserve(static_cast<int64_t>(num_elements)));
            ARROW_RETURN_NOT_OK(value_builder->Reserve(static_cast<int64_t>(num_elements) * 2));

            for (size_t elem_idx = 0; elem_idx < num_elements; elem_idx += 1) {
                const auto& element = elements[elem_idx];
                ARROW_RETURN_NOT_OK(field_builder->Append());
                if (element.groups.data()) {
                    RR_RETURN_NOT_OK(
                        Loggable<rerun::encodings::TableGroup>::fill_arrow_array_builder(
                            value_builder,
                            element.groups.data(),
                            element.groups.size()
                        )
                    );
                }
            }
        }
        {
            auto field_builder = static_cast<arrow::UInt8Builder*>(builder->field_builder(1));
            ARROW_RETURN_NOT_OK(field_builder->Reserve(static_cast<int64_t>(num_elements)));
            for (size_t elem_idx = 0; elem_idx < num_elements; elem_idx += 1) {
                const auto& element = elements[elem_idx];
                if (element.sort_scope.has_value()) {
                    RR_RETURN_NOT_OK(
                        Loggable<rerun::encodings::TableSortScope>::fill_arrow_array_builder(
                            field_builder,
                            &element.sort_scope.value(),
                            1
                        )
                    );
                } else {
                    ARROW_RETURN_NOT_OK(field_builder->AppendNull());
                }
            }
        }
        {
            auto field_builder = static_cast<arrow::UInt32Builder*>(builder->field_builder(2));
            ARROW_RETURN_NOT_OK(field_builder->Reserve(static_cast<int64_t>(num_elements)));
            for (size_t elem_idx = 0; elem_idx < num_elements; elem_idx += 1) {
                const auto& element = elements[elem_idx];
                if (element.sticky_columns.has_value()) {
                    ARROW_RETURN_NOT_OK(field_builder->Append(element.sticky_columns.value()));
                } else {
                    ARROW_RETURN_NOT_OK(field_builder->AppendNull());
                }
            }
        }
        ARROW_RETURN_NOT_OK(builder->AppendValues(static_cast<int64_t>(num_elements), nullptr));

        return Error::ok();
    }
} // namespace rerun
